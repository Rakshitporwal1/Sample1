package dbConnection;

import java.sql.Connection;

public class Program1_DbConnection {
    public static void main(String[] args) throws SQLException {
        String url = "jdbc:sqlserver://DESKTOP-A54I25O\\SQLEXPRESS01:1434;"
                + "databaseName=coforge_jan26;"
                + "user=testuser;"
                + "password=123456;"
                // + "integratedSecurity=true;"
                + "encrypt=true;"
                + "trustServerCertificate=true;";
        
        Connection con = DriverManager.getConnection(url);
        String query = "select * from employee where empid=? and empname=?";
        PreparedStatement pstmt = con.prepareStatement(query);
        
        // Statement stmt = con.createStatement();
        // int num = stmt.executeUpdate("insert into employee(empid, empname, state) val...");
        
        pstmt.setInt(1, 101);
        pstmt.setString(2, "Rajat");
        
        ResultSet result = pstmt.executeQuery();
